/**
 * Deprecated: Centralized association mapping
 *
 * Associations are now declared in each model's static associate(models) method.
 * This function is kept as a no-op for backward compatibility to avoid import errors
 * during the transition period. Do not add new associations here.
 */
export const defineAssociations = () => {
    // No-op: associations are defined per model
};
